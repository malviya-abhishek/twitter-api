

exports.routesConfig = function(app){
    app.get("/tweets", [

    ]);

    app.post("/tweets", [

    ]);

    app.delete("/tweets/:tweetId", [

    ]);

    app.patch("/tweets/:tweetId", [

    ]);

};